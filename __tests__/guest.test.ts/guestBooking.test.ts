import { describe, it } from "@jest/globals";

// Guest Booking Flow — not yet implemented (see docs/Backlog: "Guest Booking Flow").
describe("Guest Booking Flow", () => {
  it.todo("creates a booking as an authenticated guest");
  it.todo("stores guest_user_id as the booking owner");
  it.todo("stores created_by_user_id as the authenticated actor");
  it.todo("allows a guest to view their booking history");
  it.todo("allows a guest to view their booking details");
  it.todo("allows a guest to update their booking");
  it.todo("allows a guest to cancel their booking");
});
